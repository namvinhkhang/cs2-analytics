---
phase: 01-data-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .env.example
  - src/cs2_analytics/__init__.py
  - src/cs2_analytics/models/__init__.py
  - src/cs2_analytics/models/canonical.py
  - src/cs2_analytics/models/liquipedia.py
  - src/cs2_analytics/models/faceit.py
  - src/cs2_analytics/models/pandascore.py
  - src/cs2_analytics/utils/__init__.py
  - src/cs2_analytics/utils/config.py
  - src/cs2_analytics/ingestion/__init__.py
autonomous: true
requirements: [ING-08]
must_haves:
  truths:
    - "uv install succeeds and the package is importable as cs2_analytics"
    - "Settings class raises ValidationError at startup if any required env var is missing"
    - "Match, Player, Team canonical models reject payloads with missing required fields"
    - "Per-source raw models (FACEITMatch, LiquipediaTeam, PandaScoreMatch, etc.) accept extra fields silently"
    - "Canonical Match model maps source, team_a_id, team_b_id, winner_id, played_at correctly"
  artifacts:
    - path: "pyproject.toml"
      provides: "Package config, uv dependencies, pytest/mypy/ruff tool config"
      contains: "cs2_analytics"
    - path: "src/cs2_analytics/utils/config.py"
      provides: "Settings class with CS2_ env prefix"
      exports: ["Settings", "settings"]
    - path: "src/cs2_analytics/models/canonical.py"
      provides: "Match, Player, Team shared models"
      exports: ["Match", "Player", "Team"]
    - path: "src/cs2_analytics/models/faceit.py"
      provides: "FACEITMatch, FACEITPlayer source models"
      exports: ["FACEITMatch", "FACEITPlayer"]
    - path: "src/cs2_analytics/models/liquipedia.py"
      provides: "LiquipediaTeam, LiquipediaPlayer, LiquipediaMatch, LiquipediaTournament, LiquipediaPlacement source models"
      exports: ["LiquipediaTeam", "LiquipediaPlayer", "LiquipediaMatch", "LiquipediaTournament", "LiquipediaPlacement"]
    - path: "src/cs2_analytics/models/pandascore.py"
      provides: "PandaScoreMatch, PandaScorePlayer source models"
      exports: ["PandaScoreMatch", "PandaScorePlayer"]
  key_links:
    - from: "src/cs2_analytics/models/faceit.py"
      to: "src/cs2_analytics/models/canonical.py"
      via: "FACEITMatch.to_canonical() returns Match"
      pattern: "def to_canonical"
    - from: "src/cs2_analytics/utils/config.py"
      to: ".env"
      via: "SettingsConfigDict(env_file='.env', env_prefix='CS2_')"
      pattern: "env_prefix.*CS2_"
---

<objective>
Bootstrap the entire Python project structure, install all dependencies via uv, and implement the
Pydantic v2 data models that serve as the schema contract for the whole pipeline.

Purpose: Every downstream plan (ingestion clients, S3 utils, tests) depends on these models and
the config system existing first. This plan creates the foundation once so all Wave 2 plans can
build against stable interfaces.
Output: Installable src-layout Python package, Settings class, canonical models (Match, Player,
Team), and per-source raw models for FACEIT, Liquipedia, and PandaScore.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-data-ingestion/01-CONTEXT.md
@.planning/phases/01-data-ingestion/01-RESEARCH.md

<interfaces>
<!-- Canonical model field contracts all downstream code must respect. -->

Canonical Match (written to Parquet by every ingestion client):
```python
class Match(BaseModel):
    model_config = ConfigDict(extra="forbid")
    match_id: str
    source: str           # "faceit" | "liquipedia" | "pandascore" | "kaggle"
    team_a_id: str
    team_b_id: str
    winner_id: str | None
    played_at: str        # ISO-8601 date string, used as Hive partition key
    map_name: str | None = None
```

Canonical Player (written to Parquet by FACEIT and PandaScore clients):
```python
class Player(BaseModel):
    model_config = ConfigDict(extra="forbid")
    player_id: str
    source: str
    display_name: str
    team_id: str | None = None
    nationality: str | None = None
    kills: int | None = None
    deaths: int | None = None
    adr: float | None = None
    kd_ratio: float | None = None
    kast: float | None = None
    elo: int | None = None
    match_id: str | None = None   # populated for per-match stat records
    recorded_at: str              # ISO-8601 date string
```

Canonical Team (written to Parquet by Liquipedia and PandaScore clients):
```python
class Team(BaseModel):
    model_config = ConfigDict(extra="forbid")
    team_id: str
    source: str
    name: str
    region: str | None = None
    world_ranking: int | None = None
    ingested_at: str              # ISO-8601 date string
```

Settings class (CS2_ env prefix, read from .env):
```python
class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", env_prefix="CS2_")
    faceit_api_key: str
    pandascore_api_key: str
    liquipedia_api_key: str
    aws_s3_bucket: str
    aws_region: str = "us-east-1"
    kaggle_username: str
    kaggle_key: str

settings = Settings()  # module-level singleton
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold project, install dependencies, create Settings class</name>
  <files>
    pyproject.toml,
    .env.example,
    src/cs2_analytics/__init__.py,
    src/cs2_analytics/utils/__init__.py,
    src/cs2_analytics/utils/config.py,
    src/cs2_analytics/ingestion/__init__.py,
    src/cs2_analytics/models/__init__.py
  </files>
  <read_first>
    - .planning/phases/01-data-ingestion/01-RESEARCH.md (pyproject.toml template at end of file, Settings pattern in Pattern 5)
  </read_first>
  <behavior>
    - `from cs2_analytics.utils.config import settings` succeeds when CS2_* env vars are set
    - `Settings()` raises `pydantic_core.ValidationError` when CS2_FACEIT_API_KEY is absent
    - `settings.aws_region` defaults to `"us-east-1"` when CS2_AWS_REGION is not set
    - `settings.faceit_api_key` returns the value of CS2_FACEIT_API_KEY env var
  </behavior>
  <action>
    1. Create `pyproject.toml` at project root with exact content from RESEARCH.md (pyproject.toml section at bottom of file). Use `hatchling` build backend, `src/cs2_analytics` package path, `asyncio_mode = "auto"` in pytest options, strict mypy config, ruff config. Runtime deps: `httpx>=0.27`, `tenacity>=9.0`, `pydantic>=2.7`, `pydantic-settings>=2.3`, `pyarrow>=16.0`, `boto3>=1.35`, `structlog>=24.0`, `kaggle>=1.6`. Dev deps: `pytest>=8.0`, `pytest-asyncio>=0.23`, `respx>=0.21`, `ruff>=0.4`, `mypy>=1.10`, `boto3-stubs[s3]>=1.35`.

    2. Run `uv sync` to install all dependencies and create the venv.

    3. Create `src/cs2_analytics/__init__.py` as an empty file (just a docstring: `"""CS2 Pro Scene Analytics — data ingestion package."""`).

    4. Create `src/cs2_analytics/ingestion/__init__.py` — empty.

    5. Create `src/cs2_analytics/models/__init__.py` — empty.

    6. Create `src/cs2_analytics/utils/__init__.py` — empty.

    7. Create `src/cs2_analytics/utils/config.py` with `Settings(BaseSettings)` class. Use `SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", env_prefix="CS2_")`. Fields: `faceit_api_key: str`, `pandascore_api_key: str`, `liquipedia_api_key: str`, `aws_s3_bucket: str`, `aws_region: str = "us-east-1"`, `kaggle_username: str`, `kaggle_key: str`. Add module-level `settings = Settings()` instantiation. Import guard: wrap the singleton in a try/except that re-raises so tests can patch it.

    8. Create `.env.example` listing all 7 required CS2_* vars with placeholder values and comments.

    Note: Do NOT use `class Config` inner class — use `model_config = SettingsConfigDict(...)` (Pydantic v2 pattern). Never use `os.environ.get()` calls in application code.
  </action>
  <verify>
    <automated>cd /home/jerry/Project/cs2-analytics && uv run python -c "from cs2_analytics.utils.config import Settings; import pydantic_core; s = None; [exec('s = Settings()') if False else None]; print('import ok')" && uv run python -c "import cs2_analytics; print('package importable')"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `name = "cs2-analytics"` and `requires-python = ">=3.12"`
    - `pyproject.toml` contains `asyncio_mode = "auto"` under `[tool.pytest.ini_options]`
    - `src/cs2_analytics/utils/config.py` contains `env_prefix="CS2_"` (grep-verifiable)
    - `src/cs2_analytics/utils/config.py` contains `class Settings(BaseSettings)` (grep-verifiable)
    - `uv run python -c "import cs2_analytics"` exits 0
    - `.env.example` contains all 7 variable names: `CS2_FACEIT_API_KEY`, `CS2_PANDASCORE_API_KEY`, `CS2_LIQUIPEDIA_API_KEY`, `CS2_AWS_S3_BUCKET`, `CS2_AWS_REGION`, `CS2_KAGGLE_USERNAME`, `CS2_KAGGLE_KEY`
  </acceptance_criteria>
  <done>Package installs and imports cleanly; Settings class raises ValidationError on missing keys; .env.example documents all required vars.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement canonical and per-source Pydantic v2 models</name>
  <files>
    src/cs2_analytics/models/canonical.py,
    src/cs2_analytics/models/liquipedia.py,
    src/cs2_analytics/models/faceit.py,
    src/cs2_analytics/models/pandascore.py
  </files>
  <read_first>
    - src/cs2_analytics/models/__init__.py (just created — confirm empty)
    - .planning/phases/01-data-ingestion/01-RESEARCH.md (Pattern 3: Pydantic v2 Source + Canonical Model Split)
  </read_first>
  <behavior>
    - `Match(match_id="x", source="faceit", team_a_id="a", team_b_id="b", winner_id=None, played_at="2024-01-15")` succeeds
    - `Match(match_id="x", source="faceit", team_a_id="a", team_b_id="b", winner_id=None, played_at="2024-01-15", unknown_field="y")` raises ValidationError (extra="forbid" on canonical)
    - `FACEITMatch(match_id="x", game="cs2", region="EU", competition_name="Open", teams={}, unknown_extra_field="ignored")` succeeds (extra="ignore" on raw models)
    - `FACEITMatch(...).to_canonical()` returns a `Match` instance
    - `Player` with all None optional stats is valid (all stat fields are Optional)
    - `Team` with `world_ranking=None` is valid
  </behavior>
  <action>
    1. Create `src/cs2_analytics/models/canonical.py` with three classes:

       `Match(BaseModel)` — `model_config = ConfigDict(extra="forbid")`. Fields exactly as shown in the interfaces block above: `match_id: str`, `source: str`, `team_a_id: str`, `team_b_id: str`, `winner_id: str | None`, `played_at: str`, `map_name: str | None = None`.

       `Player(BaseModel)` — `model_config = ConfigDict(extra="forbid")`. Fields: `player_id: str`, `source: str`, `display_name: str`, `team_id: str | None = None`, `nationality: str | None = None`, `kills: int | None = None`, `deaths: int | None = None`, `adr: float | None = None`, `kd_ratio: float | None = None`, `kast: float | None = None`, `elo: int | None = None`, `match_id: str | None = None`, `recorded_at: str`.

       `Team(BaseModel)` — `model_config = ConfigDict(extra="forbid")`. Fields: `team_id: str`, `source: str`, `name: str`, `region: str | None = None`, `world_ranking: int | None = None`, `ingested_at: str`.

    2. Create `src/cs2_analytics/models/faceit.py` with:

       `FACEITMatch(BaseModel)` — `model_config = ConfigDict(extra="ignore")`. Fields: `match_id: str`, `game: str`, `region: str`, `competition_name: str`, `teams: dict[str, Any]`, `results: dict[str, Any] | None = None`, `finished_at: int | None = None`. Method `to_canonical(self) -> Match` — maps to canonical Match with `source="faceit"`, `team_a_id` from `self.teams["faction1"]["faction_id"]` (with fallback `"unknown"` if absent), `team_b_id` from `self.teams["faction2"]["faction_id"]` (with fallback), `winner_id` from `self.results["winner"]` if results present else `None`, `played_at` from ISO date string derived from `finished_at` epoch (use `datetime.fromtimestamp(self.finished_at, tz=timezone.utc).date().isoformat()` if present else `"unknown"`).

       `FACEITPlayer(BaseModel)` — `model_config = ConfigDict(extra="ignore")`. Fields: `player_id: str`, `nickname: str`, `country: str | None = None`, `skill_level: int | None = None`, `faceit_elo: int | None = None`. Method `to_canonical(self, *, match_id: str | None = None, kills: int | None = None, deaths: int | None = None, adr: float | None = None, kd_ratio: float | None = None, kast: float | None = None, recorded_at: str) -> Player` — maps to canonical Player.

    3. Create `src/cs2_analytics/models/liquipedia.py` with five classes all using `ConfigDict(extra="ignore")`:

       `LiquipediaTeam(BaseModel)` — fields: `pagename: str`, `name: str`, `region: str | None = None`. Method `to_canonical(self, *, ingested_at: str) -> Team` mapping `team_id=self.pagename`, `source="liquipedia"`, `name=self.name`, `region=self.region`, `ingested_at=ingested_at`.

       `LiquipediaPlayer(BaseModel)` — fields: `pagename: str`, `id: str`, `name: str | None = None`, `nationality: str | None = None`, `teampagename: str | None = None`. Method `to_canonical(self, *, recorded_at: str) -> Player` mapping `player_id=self.pagename`, `source="liquipedia"`, `display_name=self.id`, `team_id=self.teampagename`, `nationality=self.nationality`, `recorded_at=recorded_at`.

       `LiquipediaMatch(BaseModel)` — fields: `match2id: str`, `team1: str | None = None`, `team2: str | None = None`, `winner: str | None = None`, `date: str | None = None`, `tournament: str | None = None`. Method `to_canonical(self) -> Match` mapping `match_id=self.match2id`, `source="liquipedia"`, `team_a_id=self.team1 or "unknown"`, `team_b_id=self.team2 or "unknown"`, `winner_id=self.winner`, `played_at=self.date or "unknown"`.

       `LiquipediaTournament(BaseModel)` — fields: `pagename: str`, `name: str`, `startdate: str | None = None`, `enddate: str | None = None`, `prizepool: str | None = None`, `tier: str | None = None`.

       `LiquipediaPlacement(BaseModel)` — fields: `pagename: str`, `tournament: str | None = None`, `place: str | None = None`, `team: str | None = None`.

    4. Create `src/cs2_analytics/models/pandascore.py` with:

       `PandaScoreMatch(BaseModel)` — `model_config = ConfigDict(extra="ignore")`. Fields: `id: int`, `name: str`, `status: str`, `begin_at: str | None = None`, `end_at: str | None = None`, `winner: dict[str, Any] | None = None`, `opponents: list[dict[str, Any]] = []`. Method `to_canonical(self) -> Match` — maps `match_id=str(self.id)`, `source="pandascore"`, `team_a_id=str(self.opponents[0]["opponent"]["id"]) if len(self.opponents) > 0 else "unknown"`, `team_b_id=str(self.opponents[1]["opponent"]["id"]) if len(self.opponents) > 1 else "unknown"`, `winner_id=str(self.winner["id"]) if self.winner else None`, `played_at=self.begin_at[:10] if self.begin_at else "unknown"`.

       `PandaScorePlayer(BaseModel)` — `model_config = ConfigDict(extra="ignore")`. Fields: `id: int`, `name: str`, `nationality: str | None = None`, `current_team: dict[str, Any] | None = None`. Method `to_canonical(self, *, kills: int | None = None, deaths: int | None = None, adr: float | None = None, kd_ratio: float | None = None, match_id: str | None = None, recorded_at: str) -> Player`.

    All files must use `from __future__ import annotations` at top. All methods must have full type hints. Use `from typing import Any` for dict fields. Import canonical models within method bodies (not at module level) to avoid circular imports.
  </action>
  <verify>
    <automated>cd /home/jerry/Project/cs2-analytics && uv run python -c "
from cs2_analytics.models.canonical import Match, Player, Team
from cs2_analytics.models.faceit import FACEITMatch, FACEITPlayer
from cs2_analytics.models.liquipedia import LiquipediaTeam, LiquipediaMatch
from cs2_analytics.models.pandascore import PandaScoreMatch
import pydantic_core

# canonical extra=forbid
try:
    Match(match_id='x', source='faceit', team_a_id='a', team_b_id='b', winner_id=None, played_at='2024-01-01', bad_field='y')
    raise AssertionError('should have raised')
except pydantic_core.ValidationError:
    pass

# raw extra=ignore
m = FACEITMatch(match_id='abc', game='cs2', region='EU', competition_name='Open', teams={}, extra_undocumented='ignored')
print('all model checks passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "extra=\"forbid\"" src/cs2_analytics/models/canonical.py` returns at least 3 matches (Match, Player, Team)
    - `grep -r "extra=\"ignore\"" src/cs2_analytics/models/faceit.py src/cs2_analytics/models/liquipedia.py src/cs2_analytics/models/pandascore.py` returns at least 5 matches
    - `grep -r "def to_canonical" src/cs2_analytics/models/` returns matches in faceit.py, liquipedia.py (LiquipediaTeam, LiquipediaPlayer, LiquipediaMatch), pandascore.py
    - `grep "class Config" src/cs2_analytics/models/canonical.py src/cs2_analytics/models/faceit.py` returns empty (no v1 style Config)
    - Verification script above exits 0 with "all model checks passed"
  </acceptance_criteria>
  <done>All canonical models enforce extra="forbid"; all source models tolerate extra fields; to_canonical() methods exist and return typed canonical instances.</done>
</task>

</tasks>

<verification>
Run after both tasks complete:
```bash
cd /home/jerry/Project/cs2-analytics
uv run python -c "import cs2_analytics; from cs2_analytics.models.canonical import Match, Player, Team; from cs2_analytics.utils.config import Settings; print('Phase 01-01 foundation verified')"
uv run mypy src/cs2_analytics/models/ src/cs2_analytics/utils/ --ignore-missing-imports
```
</verification>

<success_criteria>
- `uv sync` completes without errors
- `import cs2_analytics` succeeds
- All canonical models use `extra="forbid"` and all source models use `extra="ignore"`
- Every source model has a `to_canonical()` method returning the appropriate canonical type
- `Settings` class reads from `CS2_` prefixed env vars
- No `class Config` inner classes anywhere (Pydantic v2 style throughout)
- mypy passes on models/ and utils/ with no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-ingestion/01-01-SUMMARY.md` documenting:
- Files created and their exports
- Exact canonical model field names (copy the dataclass signatures — Wave 2 plans need these)
- pyproject.toml dependency versions actually installed (uv.lock resolved versions)
- Any deviations from the plan and why
</output>
