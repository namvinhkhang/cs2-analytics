---
phase: 01-data-ingestion
plan: 08
type: execute
wave: 5
depends_on: [01-07]
files_modified:
  - src/cs2_analytics/ingestion/base.py
  - src/cs2_analytics/ingestion/kaggle.py
  - src/cs2_analytics/ingestion/liquipedia.py
  - src/cs2_analytics/models/faceit.py
  - src/cs2_analytics/models/liquipedia.py
  - src/cs2_analytics/models/pandascore.py
  - src/cs2_analytics/utils/parquet.py
  - tests/conftest.py
  - tests/test_base_client.py
  - tests/test_config.py
  - tests/test_faceit_client.py
  - tests/test_kaggle_ingester.py
  - tests/test_liquipedia_client.py
  - tests/test_parquet.py
  - tests/test_s3.py
autonomous: true
requirements: [ING-01, ING-02, ING-03, ING-04, ING-05, ING-06, ING-07, ING-08]
gap_closure: true

must_haves:
  truths:
    - "uv run ruff check src/ tests/ exits with 0 (no errors)"
    - "uv run pytest exits with 0 (all 147 tests pass after lint fixes)"
    - "uv run mypy src/cs2_analytics/ exits with 0 (type checks clean)"
  artifacts:
    - path: "src/cs2_analytics/ingestion/base.py"
      provides: "BaseAPIClient with UP035-corrected AsyncIterator import from collections.abc"
    - path: "src/cs2_analytics/models/pandascore.py"
      provides: "E501 comment line split to <= 100 chars"
    - path: "tests/test_faceit_client.py"
      provides: "E501 docstring split, unused imports removed, F841 fixed"
    - path: "tests/test_parquet.py"
      provides: "E501 list literal broken across lines"
  key_links:
    - from: "ruff check src/ tests/"
      to: "exit code 0"
      via: "--fix for 38 auto-fixable + manual edits for 4 E501 lines"
      pattern: "ruff check.*exit 0"
    - from: "uv run pytest"
      to: "147 passed"
      via: "no functional changes — only import cleanup and formatting"
      pattern: "147 passed"
---

<objective>
Fix all 45 ruff lint errors in src/ and tests/ so the codebase satisfies the project's
configured ruff rules (ruff.toml / pyproject.toml). Apply ruff format for consistent style.
Confirm all 147 tests still pass after the mechanical cleanup.

Purpose: Phase 1 success criteria require a clean test suite and lint-clean source. The 45
errors block a green CI gate and are the sole remaining gap between the current codebase and
a fully shippable Phase 1.

Output: Zero ruff errors across src/ and tests/; 147 tests green; formatted source committed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Error inventory (confirmed via uv run ruff check src/ tests/ on 2026-03-16):

AUTO-FIXABLE WITH --fix (38 errors):
  UP035  src/cs2_analytics/ingestion/base.py:16       — AsyncIterator from typing → collections.abc
  F401   src/cs2_analytics/ingestion/kaggle.py:18     — unused import: os
  F401   src/cs2_analytics/ingestion/kaggle.py:22     — unused import: typing.Any
  F401   src/cs2_analytics/ingestion/liquipedia.py:20 — unused import: typing.Any
  UP037  src/cs2_analytics/models/faceit.py:27        — remove quotes from return type "Match"
  UP017  src/cs2_analytics/models/faceit.py:47        — timezone.utc → datetime.UTC
  UP037  src/cs2_analytics/models/faceit.py:81        — remove quotes from return type "Player"
  F401   src/cs2_analytics/models/liquipedia.py:10    — unused import: typing.Any
  UP037  src/cs2_analytics/models/liquipedia.py:22    — remove quotes from return type "Team"
  UP037  src/cs2_analytics/models/liquipedia.py:46    — remove quotes from return type "Player"
  UP037  src/cs2_analytics/models/liquipedia.py:72    — remove quotes from return type "Match"
  UP037  src/cs2_analytics/models/pandascore.py:26    — remove quotes from return type "Match"
  UP037  src/cs2_analytics/models/pandascore.py:77    — remove quotes from return type "Player"
  I001   src/cs2_analytics/utils/parquet.py           — unsorted import block
  I001   tests/conftest.py                             — unsorted import block
  I001   tests/test_base_client.py                    — unsorted import block
  F401   tests/test_base_client.py:10                 — unused: unittest.mock.patch
  I001   tests/test_config.py                         — unsorted import blocks (×2)
  F401   tests/test_faceit_client.py:21               — unused: unittest.mock.MagicMock
  F841   tests/test_faceit_client.py:190              — mock_write assigned but never used
  F401   tests/test_faceit_client.py:320              — unused: cs2_analytics.models.canonical.Match
  I001   tests/test_faceit_client.py                  — unsorted import block
  I001   tests/test_kaggle_ingester.py                — unsorted import block
  F401   tests/test_kaggle_ingester.py:18             — unused: os
  F401   tests/test_kaggle_ingester.py:20             — unused: tempfile
  F401   tests/test_kaggle_ingester.py:23             — unused: MagicMock, call
  F841   tests/test_kaggle_ingester.py:270            — count assigned but never used
  F841   tests/test_liquipedia_client.py:248          — mock_s3 assigned but never used
  F401   tests/test_liquipedia_client.py:21           — unused: MagicMock
  I001   tests/test_parquet.py (multiple)             — unsorted import blocks
  F401   tests/test_parquet.py:11                     — unused: pytest
  I001   tests/test_s3.py                             — unsorted import blocks
  F401   tests/test_s3.py:11                          — unused: pyarrow
  F401   tests/test_s3.py:13                          — unused: pytest

NOT AUTO-FIXABLE (4 E501 errors — must split lines manually):
  src/cs2_analytics/models/pandascore.py:43   — 105 chars: inline comment on datetime extract line
  tests/test_faceit_client.py:299             — 102 chars: docstring line too long
  tests/test_liquipedia_client.py:157         — 101 chars: docstring line too long
  tests/test_parquet.py:52                    — 113 chars: list literal

Fix instructions for E501:
  pandascore.py:43 — move inline comment to line above as its own comment line
  test_faceit_client.py:299 docstring — wrap at "not call" → break into two lines
  test_liquipedia_client.py:157 docstring — wrap at "LiquipediaTournament" → break
  test_parquet.py:52 — break list literal across two lines at the "elo" element
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run ruff --fix for 38 auto-fixable errors</name>
  <files>
    src/cs2_analytics/ingestion/base.py
    src/cs2_analytics/ingestion/kaggle.py
    src/cs2_analytics/ingestion/liquipedia.py
    src/cs2_analytics/models/faceit.py
    src/cs2_analytics/models/liquipedia.py
    src/cs2_analytics/models/pandascore.py
    src/cs2_analytics/utils/parquet.py
    tests/conftest.py
    tests/test_base_client.py
    tests/test_config.py
    tests/test_faceit_client.py
    tests/test_kaggle_ingester.py
    tests/test_liquipedia_client.py
    tests/test_parquet.py
    tests/test_s3.py
  </files>
  <read_first>
    - src/cs2_analytics/ingestion/base.py (UP035: AsyncIterator import)
    - src/cs2_analytics/models/faceit.py (UP037 × 2, UP017)
    - src/cs2_analytics/models/liquipedia.py (F401, UP037 × 3)
    - src/cs2_analytics/models/pandascore.py (UP037 × 2 — do NOT touch line 43 E501 here)
    - tests/test_faceit_client.py (F401, F841, I001 — do NOT touch line 299 E501 here)
    - tests/test_kaggle_ingester.py (F401 × 4, F841, I001)
    - tests/test_liquipedia_client.py (F401, F841 — do NOT touch line 157 E501 here)
    - tests/test_parquet.py (F401, I001 × 3 — do NOT touch line 52 E501 here)
    - tests/test_s3.py (F401 × 2, I001)
  </read_first>
  <action>
    Run the ruff auto-fixer:

      uv run ruff check src/ tests/ --fix

    This handles all 38 auto-fixable errors:
    - UP035 (AsyncIterator import moved from typing to collections.abc in base.py)
    - UP037 (remove string quotes from return type annotations in model files)
    - UP017 (timezone.utc → datetime.UTC in faceit.py)
    - F401 (remove unused imports in kaggle.py, liquipedia.py, models, test files)
    - F841 (remove/suppress unused local variable assignments in test files)
    - I001 (sort and format import blocks in all affected files)

    After running --fix, verify the remaining count is exactly 4 (E501 only):

      uv run ruff check src/ tests/ 2>&1 | grep "^Found"
      # Expected: "Found 4 errors."

    If F841 (mock_write, mock_s3, count) are not auto-fixed, suppress them manually:
    - tests/test_faceit_client.py:190 — change `mock_write` context manager to use `_` as variable name
    - tests/test_kaggle_ingester.py:270 — change `count = ingester.ingest_csv_file(...)` to `_ = ingester.ingest_csv_file(...)`
    - tests/test_liquipedia_client.py:248 — change `mock_s3` to `_`
  </action>
  <verify>
    <automated>uv run ruff check src/ tests/ 2>&1 | grep "^Found" | grep -v "^Found 4 errors"</automated>
    <!-- Expected: no output (only 4 E501 remain) -->
  </verify>
  <acceptance_criteria>
    - `uv run ruff check src/ tests/ 2>&1 | grep "^Found"` outputs exactly "Found 4 errors."
    - All 4 remaining errors are E501 (confirmed by `uv run ruff check src/ tests/ --select E501`)
    - No new errors introduced (no UP, F, I codes remain)
    - `uv run pytest --tb=short -q` still shows 147 passed (no test breakage from import removals)
  </acceptance_criteria>
  <done>38 auto-fixable lint errors resolved; 4 E501 remain; all 147 tests still pass</done>
</task>

<task type="auto">
  <name>Task 2: Fix 4 E501 line-too-long errors manually then run ruff format</name>
  <files>
    src/cs2_analytics/models/pandascore.py
    tests/test_faceit_client.py
    tests/test_liquipedia_client.py
    tests/test_parquet.py
  </files>
  <read_first>
    - src/cs2_analytics/models/pandascore.py (read line 43 context to plan the split)
    - tests/test_faceit_client.py (read line 299 docstring context)
    - tests/test_liquipedia_client.py (read line 157 docstring context)
    - tests/test_parquet.py (read line 52 list literal context)
  </read_first>
  <action>
    Fix each E501 violation — each file needs one targeted edit:

    1. src/cs2_analytics/models/pandascore.py line 43 (105 chars):
       Current:  `        # Extract date portion from ISO-8601 datetime string (e.g. "2024-01-15T12:00:00Z" → "2024-01-15")`
       Fix: Move to two lines — split after "ISO-8601 datetime string":
         ```
         # Extract date portion from ISO-8601 datetime string
         # (e.g. "2024-01-15T12:00:00Z" → "2024-01-15")
         ```

    2. tests/test_faceit_client.py line 299 (102 chars):
       Current docstring: `"""ingest_matches() with empty match_ids should return (0, 0) and not call write_parquet_to_s3."""`
       Fix: Split at "not call":
         ```
         """ingest_matches() with empty match_ids should return (0, 0)
         and not call write_parquet_to_s3."""
         ```

    3. tests/test_liquipedia_client.py line 157 (101 chars):
       Current docstring: `"""get_tournaments() deserializes the {result: [...]} envelope into LiquipediaTournament list."""`
       Fix: Split after "envelope":
         ```
         """get_tournaments() deserializes the {result: [...]} envelope
         into LiquipediaTournament list."""
         ```

    4. tests/test_parquet.py line 52 (113 chars):
       Current: `    nullable_fields = ["team_id", "nationality", "kills", "deaths", "adr", "kd_ratio", "kast", "elo", "match_id"]`
       Fix: Break the list across two lines:
         ```
         nullable_fields = [
             "team_id", "nationality", "kills", "deaths", "adr",
             "kd_ratio", "kast", "elo", "match_id",
         ]
         ```

    After all four manual edits, run ruff format for consistent style:

      uv run ruff format src/ tests/

    Then confirm zero lint errors remain:

      uv run ruff check src/ tests/
      # Expected: "All checks passed."
  </action>
  <verify>
    <automated>uv run ruff check src/ tests/ && uv run pytest --tb=short -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ruff check src/ tests/` exits with 0 and prints "All checks passed." (or no output)
    - `uv run pytest --tb=short -q` shows "147 passed" (no test regressions)
    - `uv run ruff format src/ tests/` produces no changes on a second run (idempotent — formatting already applied)
    - `uv run mypy src/cs2_analytics/` exits with 0 (UP037 string-annotation removal does not break mypy)
    - grep confirms no UP035/UP037/UP017/F401/F841/I001/E501 codes remain:
      `uv run ruff check src/ tests/ --select UP035,UP037,UP017,F401,F841,I001,E501 2>&1 | grep "error\|Found"`
      — expected: no output or "All checks passed."
  </acceptance_criteria>
  <done>Zero ruff errors; ruff format applied; 147 tests green; mypy clean</done>
</task>

</tasks>

<verification>
After both tasks complete, the following commands must all succeed:

```bash
# Lint clean
uv run ruff check src/ tests/
# Expected: All checks passed. (exit 0)

# Format idempotent
uv run ruff format --check src/ tests/
# Expected: All checks passed. (exit 0)

# Tests green
uv run pytest --tb=short -q
# Expected: 147 passed in ~18s

# Type checks clean
uv run mypy src/cs2_analytics/
# Expected: Success: no issues found
```
</verification>

<success_criteria>
Phase 1 gap closure is complete when ALL of the following are true:

1. `uv run ruff check src/ tests/` exits 0 with no reported errors
2. `uv run ruff format --check src/ tests/` exits 0 (format already applied, no drift)
3. `uv run pytest -q` reports exactly 147 passed, 0 failed, 0 errors
4. `uv run mypy src/cs2_analytics/` exits 0
5. No functional logic was changed — only imports cleaned, annotations un-quoted, lines wrapped
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-ingestion/01-08-SUMMARY.md`

The summary should record:
- Which ruff error codes were fixed and how many of each
- Confirmation that all 147 tests passed after cleanup
- Confirmation that mypy exits clean
- Any edge cases encountered during the F841 or UP037 fixes
</output>
